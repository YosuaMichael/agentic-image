# Pull request

## What changed (skill / script / docs)?

## JSON-contract impact (none, or `<name>/vN` bump + CHANGELOG entry)

## Gates (`ruff check .`, `pytest tests/` — paste results)

## `git status` check (no `oss/` / sessions / weights / keys staged)
